<p>ライブ編集</p>

	<%= form_for(@live) do |f| %>
	<%= live.artist_id.artist_name %>
  <%= f.text_date :when %>
  <%= f.select :place_id, Place.all.collect { |p| [ p.place, p.id ] } %>
  <%= f.text_field :live_title %>

	<%= f.submit '追加' %>
	<% end %>
	<%= link_to "戻る", artist_path(@live.artist_id) %>